# Screenshots Folder

Place the following 6 screenshots here before submission (see Section 10 of `Solution_Complete.md` for exact capture instructions):

1. `Formula_Field.png` — Days_Until_Close__c field definition + sample record value
2. `Report_1.png` — Open Pipeline report, run mode, with Stage subtotals
3. `Report_2.png` — Win Rate by Owner report, run mode, with Win Rate % column
4. `Report_3.png` — At-Risk Opportunities report, run mode, negative Days Until Close visible
5. `Dashboard.png` — Full dashboard, all 4 components, live data
6. `Sharing_Rule.png` — Dashboard folder sharing screen showing Sales Team Role → Viewer

This README is a placeholder so the folder isn't empty in version control. Delete it once screenshots are added, or leave it — either is fine for submission.
